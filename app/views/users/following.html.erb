<%= link_to :back do %>
    <div class="mt-3 ml-3 back-btn"> < 戻る</div>
<% end %>

<div class="text-center user-index-title mb-3"><%= @title %></div>
<%= render partial: 'index'%>

<% if @users.empty? %>
    <div class="when-not-text-follow text-center">
        <%= @when_not_text %>
    </div>
<% end %>
<div class="pagenation">
    <%= paginate @users%>
</div>