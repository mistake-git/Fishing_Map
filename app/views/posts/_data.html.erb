<div class="my-3 text-secondary d-flex justify-content-center">
 <div class="mx-2"><i class="fas fa-heart"></i><%= post.likes.count %></div>
 <div class="mx-2"><i class="fas fa-comment"><%= post.comments.count %></i></div>
 <div class="mx-2"><%= "#{time_ago_in_words(post.created_at)}前"%></div>
</div>